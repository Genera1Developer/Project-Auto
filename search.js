"//Focus the search input when the page is loaded\ndocument.getElementById('search-input').focus();\n\n// Execute a search when either the search button is pressed or enter is pressed in the search input\ndocument.getElementById('search-button').addEventListener('click', performSearch);\ndocument.getElementById('search-input').addEventListener('keypress', function(event) { if (event.key === 'Enter') { performSearch(); } });\n\nfunction performSearch() {\n  const searchTerm = document.getElementById('search-input').value;\n  window.location.href = \`/search/${encodeURIComponent(searchTerm)}\`;\n}"