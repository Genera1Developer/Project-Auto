{
  "name": "project-auto",
  "version": "1.0.0",
  "description": "Project Auto website",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "build": "npm install && npm install --prefix public && npm run build --prefix public"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "node-fetch": "^3.3.2",
    "octokit": "^3.1.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}