file path: api/package.json
content: {
  "name": "web-proxy",
  "version": "1.0.0",
  "description": "A simple web proxy server",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "cheerio": "^1.0.0-rc.10",
    "express": "^4.18.2",
    "express-rate-limit": "^5.4.2"
  }
}
file path: api/proxy.js
content: const express = require('express');
const axios = require('axios');
const cheerio = require('cheerio');
const rateLimit = require('express-rate-limit');

const app = express();

// Implement rate limiting
const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 minutes
  max: 100, // limit each IP to 100 requests per windowMs
});
app.use(limiter);

app.get('/', (req, res) => {
  const url = req.query.url;
  if (!url) {
    return res.status(400).send('Missing URL parameter');
  }

  // Fetch the HTML content of the URL
  axios.get(url)
    .then(response => {
      // Parse the HTML content
      const $ = cheerio.load(response.data);

      // Remove any unnecessary elements
      $('script').remove();
      $('style').remove();

      // Send the modified HTML content back to the client
      res.send($.html());
    })
    .catch(error => {
      res.status(500).send('Error fetching URL');
    });
});

app.listen(3000, () => {
  console.log('Web proxy server running on port 3000');
});