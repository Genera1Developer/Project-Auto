FILE PATH: middleware/validate-request.js
CONTENT: 

```javascript
const validateRequest = (req, res, next) => {
  const { url } = req.body;
  if (!url) {
    return res.status(400).json({ error: 'Missing URL parameter' });
  }

  next();
};

module.exports = validateRequest;
```