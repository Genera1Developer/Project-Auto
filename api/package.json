FILE PATH: api/index.js
CONTENT: 

```javascript
const express = require('express');
const app = express();

app.use(express.json());

app.use('/http', require('./routes/http'));

app.listen(3000, () => {
  console.log('Server listening on port 3000');
});
```