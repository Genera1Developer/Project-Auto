improved file:

```json
{
  "watch": ["app.js", "utils/*.js"],
  "exec": "node app.js",
  "env": {
    "NODE_ENV": "development"
  },
  "ignore": ["node_modules/**"]
}
```

new file:

```sh
# Run script
npm run dev
```

file: api/app.js

improved file:

```js
const express = require('express');
const open = require('open');
const app = express();

app.use(express.static('public'));

app.listen(3000, () => {
  console.log('Server listening on port 3000');
  open('http://localhost:3000');
});
```