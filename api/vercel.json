Based on the project goal, the following file should be created:

FILE PATH: api/handler.js
CONTENT: 

```js
const proxy = require('./proxy');

module.exports = (req, res) => {
  if (req.method === 'POST') {
    proxy(req, res);
  } else {
    res.status(405).send('Method Not Allowed');
  }
};
```