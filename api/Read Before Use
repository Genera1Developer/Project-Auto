## File Structure

- `README.md`
- `api/proxy.js`
- `api/config.js`

## Modified `api/proxy.js`

```javascript
addEventListener('fetch', event => {
  event.respondWith(handleRequest(event.request));
});

async function handleRequest(request) {
  const { redirectUrl } = await getRedirectUrl(request);

  if (!redirectUrl) {
    return new Response('Invalid URL', {
      status: 400,
      headers: { 'Content-Type': 'text/plain' },
    });
  }

  // Replace spaces with encoded space character
  const encodedUrl = redirectUrl.replace(/ /g, '%20');

  return fetch(encodedUrl, {
    redirect: 'follow',
    headers: {
      'referer': 'https://cdn.discordapp.com',
      'Host': request.headers.get('Host'),
    },
  });
}

async function getRedirectUrl(request) {
  let url = new URL(request.url);
  const redirec = url.searchParams.get('url');

  return { redirectUrl: redirec };
}
```

## New File: `api/config.js`

```javascript
const config = {
  // Add your CDN URLs here.
  cdnUrls: [
    'cdn.example.com',
    'another-cdn.example.com',
  ],
};

export default config;
```

## `api/Read Before Use`

```
This file is not used in the modified codebase.
```